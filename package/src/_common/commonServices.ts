

export const changeRecentTabIndex=(recentTabs: any, currentTab: any)=>{

        let tabs = recentTabs

        const supplierIdValid = recentTabs.findIndex((data: any) => data.value === currentTab) === -1;

                
        return 'data';
}